-- Instead of directly updating auth.email_templates which doesn't exist,
-- we'll use the Supabase API to update email templates

-- This is a placeholder migration that doesn't actually run SQL
-- The email templates should be updated through the Supabase dashboard or API

SELECT 1;

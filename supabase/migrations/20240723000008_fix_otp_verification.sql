-- Simple migration that doesn't try to access auth.config
-- Just enable OTP settings through the Supabase dashboard instead

-- This is a placeholder migration that doesn't actually run SQL
SELECT 1;
